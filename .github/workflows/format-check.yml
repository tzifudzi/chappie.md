name: Format Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  format:
    name: Check markdown formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install -g prettier@3.1.0 markdownlint-cli

      - name: Check formatting (.md and .qmd)
        run: prettier --check "**/*.md" "**/*.qmd"

      - name: Lint markdown
        run: markdownlint "**/*.md" "**/*.qmd"
